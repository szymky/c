# C

small lil c compiler (lexer and parser for now)

## How to use

> With nix
```sh
git clone https://github.com/szymky/c.git
cd c
nix develop
cargo run
```

> NOTE: without nix, you must have the rust toolchain installed.
